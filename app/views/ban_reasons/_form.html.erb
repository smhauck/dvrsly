<%= form_with(model: ban_reason) do |form| %>
  <% if ban_reason.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ban_reason.errors.count, "error") %> prohibited this ban_reason from being saved:</h2>

      <ul>
        <% ban_reason.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.textarea :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
